import Model from './model.js';

export default class HardwareVersions extends Model {
    static type = 'hardware-versions---'; // TODO: Remove the garbage characters
    static attribute_types = {};
    static relationship_types = {};
}
